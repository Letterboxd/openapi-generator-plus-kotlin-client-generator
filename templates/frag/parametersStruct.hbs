data class {{{className (concat name '_' 'request')}}}(
{{#each parameters}}

    {{>frag/propertyDocumentation}}
    val {{{name}}}: {{{nativeType}}}{{#unless required}} = {{{undefinedValueLiteral schema}}}{{/unless}},
{{/each}}

)
