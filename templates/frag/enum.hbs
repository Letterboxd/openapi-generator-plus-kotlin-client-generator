{{>frag/schemaDocumentation}}
@Serializable
enum class {{{name}}}(val value: kotlin.String) {
{{#each enumValues}}
    @SerialName(value = {{{literalValue}}}) {{{name}}}({{{literalValue}}}){{#unless @last}},{{else}};{{/unless}}
{{/each}}
}