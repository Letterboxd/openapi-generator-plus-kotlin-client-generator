{{#each schemas}}
{{#if (isEnum)}}
{{>frag/enum supportPackage=../supportPackage}}

{{else if (isInterface)}}
{{>frag/interface}}

{{else if (isObject)}}
{{>frag/pojo supportPackage=../supportPackage}}

{{else if (isOneOf)}}
{{>frag/oneOf}}

{{/if}}
{{/each}}
